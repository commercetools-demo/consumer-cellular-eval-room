[[redirects]]
  from = "/_tracker/*"
  to = "https://tracker.ctdemo.net/:splat"
  status = 200
  force = true

# Access gate disabled for now — re-enable by uncommenting the edge_functions block
# when a password gate is ready to ship.
# [[edge_functions]]
#   path = "/*"
#   function = "demo-tracker-gate"
